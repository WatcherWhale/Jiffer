name: Test compilation of all projects

on:
  push:
    branches: [ master, workflows ]
  pull_request:
    branches: [ master, workflows ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Build 
        run: |
          cd backend
          npm install
          npm run build
